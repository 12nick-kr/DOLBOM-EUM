# 돌봄이음 AI — 디자인 시스템

> 기준: `PRD_돌봄이음_AI.md` v1.3
> 적용 화면: `/senior`(노인), `/family`(부양가족), `/worker`(사회복지사)

이 문서는 **규칙만** 정의한다. 화면별 픽셀 배치는 정의하지 않는다.
구현자는 아래 토큰과 레이아웃 규칙을 지키는 한, 컴포넌트 조합은 자유롭게 결정한다.

---

## 1. 원칙

1. **한 화면에 한 가지 일.** 화면마다 주요 행동 1개, 보조 행동은 최대 2개.
2. **테두리로 나누고, 그림자는 쓰지 않는다.** 카드 구분은 1px 선. 떠 있는 요소(모달·토스트)만 예외.
3. **색은 의미일 때만 쓴다.** 장식용 색·그라데이션·이미지 배경 금지.
4. **크기는 토큰으로만 지정한다.** `16px`, `1.2rem` 같은 임의값을 컴포넌트에 직접 쓰지 않는다.
5. **역할별로 다른 디자인을 만들지 않는다.** 세 화면은 같은 토큰을 쓰고, `/senior`만 밀도 스케일을 크게 잡는다.

---

## 2. 토큰

### 2.1 색

```css
--blue:        #3182F6;  /* 기본 액션 */
--blue-strong: #1B64DA;  /* hover/press */
--blue-bg:     #EBF2FE;  /* 선택·AI 태그 배경 */

--red:   #F04452;  --red-bg:   #FFEDEE;   /* 긴급 */
--amber: #FF9F1A;  --amber-bg: #FFF4E1;   /* 주의·대기 */
--mint:  #01C88E;  --mint-bg:  #E3FBF3;   /* 안정·완료 */

--ink:   #191F28;  /* 제목·본문 */
--ink-2: #4E5968;  /* 보조 텍스트 */
--ink-3: #8B95A1;  /* 메타·placeholder */

--line:    #E5E8EB;  /* 테두리·구분선 */
--surface: #FFFFFF;  /* 카드 */
--bg:      #F9FAFB;  /* 페이지 배경 */
--bg-alt:  #F2F4F6;  /* 카드 안쪽 블록·비활성 */
```

브랜드 블루 1개 + 시맨틱 3개가 전부다. 새 색을 추가하지 않는다.

### 2.2 타이포

크기는 **역할 이름으로만** 부른다. 컴포넌트 코드나 리뷰에서 "16px로 해주세요" 같은 절대값 지정은 하지 않는다.

| 역할 | 용도 |
|---|---|
| `display` | 화면 제목 (화면당 1개) |
| `title` | 카드·섹션 제목 |
| `body` | 본문, 버튼, 입력값 |
| `label` | 라벨, 배지, 메타 정보 |

- 폰트: Pretendard, 폴백 `-apple-system, 'Apple SD Gothic Neo', 'Malgun Gothic', sans-serif`
- 굵기는 **두 단계만** 쓴다: 본문 600, 강조·제목·버튼 700.
- 실제 수치는 `app/globals.css`의 `--text-*` 변수 한 곳에만 둔다. 밀도 스케일(§2.4)이 이 값을 바꾼다.

### 2.3 간격·형태

4의 배수 스케일만 쓴다: `4 · 8 · 12 · 16 · 24 · 32 · 48`

| 토큰 | 용도 |
|---|---|
| `--radius-card` | 카드·시트 |
| `--radius-control` | 버튼·입력 |
| `--radius-pill` | 배지 (999px) |
| `--gap-card` | 카드 내부 패딩 |
| `--gap-stack` | 카드 사이 세로 간격 |
| `--gap-page` | 화면 좌우 여백 |

### 2.4 밀도 스케일

같은 컴포넌트를 두 밀도로 쓴다. 별도 컴포넌트를 만들지 않는다.

| 밀도 | 적용 | 규칙 |
|---|---|---|
| `default` | `/family`, `/worker` | 기준값 |
| `comfort` | `/senior` | 글자·터치 타깃·간격을 한 단계 크게. 최소 터치 영역 48×48, 주요 버튼은 화면 폭에 맞춰 더 크게 |

`comfort`는 루트에 `data-density="comfort"`를 걸어 CSS 변수만 교체하는 방식으로 구현한다.

---

## 3. 레이아웃

### 3.1 공통

- **세로 1열 스택이 기본이다.** 카드는 위에서 아래로 쌓이고 `--gap-stack`으로만 띄운다.
- 카드 안에서도 요소는 세로로 쌓는다. 가로 배치는 `라벨 ↔ 값`, `제목 ↔ 배지` 같은 2단 대비에만 쓴다.
- 절대 위치(`position: absolute`)는 **화면 고정 요소에만** 쓴다: 노인 화면 긴급 버튼, 하단 내비, 모달.
- 한 화면에서 카드 폭은 모두 같다. 폭이 다른 카드를 섞지 않는다.

### 3.2 화면 골격

| 화면 | 골격 |
|---|---|
| `/senior` | 모바일 1열. 상단 인사 → 주요 행동 → 카드 스택. 긴급 버튼은 항상 같은 자리에 고정 |
| `/family` | 모바일 1열. 긴급 알림이 있을 때만 최상단에 카드 1개 추가 |
| `/worker` | 데스크톱 2열: 좌측 고정 사이드바 + 본문 카드 그리드. 폭이 좁아지면 1열로 접힌다 |

`/worker`의 카드 그리드는 CSS Grid `repeat(auto-fill, minmax(...))` 한 가지 규칙으로만 배치한다. 카드마다 다른 `grid-column` 값을 손으로 지정하지 않는다.

---

## 4. 컴포넌트 규칙

| 컴포넌트 | 규칙 |
|---|---|
| 카드 | `--surface` 배경 + 1px `--line` 테두리 + `--radius-card`. 그림자 없음 |
| 1차 버튼 | `--blue` 배경 / 흰 글자. 화면당 1개 |
| 2차 버튼 | `--bg-alt` 배경 / `--ink` 글자. 테두리 없음 |
| 위험 버튼 | `--red` 배경. `119 전화하기` 등 되돌릴 수 없는 행동에만 |
| 상태 배지 | 시맨틱 배경(`*-bg`) + 시맨틱 글자, `--radius-pill`. 텍스트 없이 색만으로 상태를 표현하지 않는다 |
| AI 배지 | `--blue-bg` 배경 + `AI` 라벨. **실제로 AI가 생성한 것에만** 붙인다 |
| 음성 컨트롤 | `다시 듣기` / `일시정지` / `그만 듣기` 3개를 항상 한 세트로, 답변 카드 하단 같은 자리에 |
| 요청 카드 | §5 |

빈 상태·로딩·오류는 카드 자리를 그대로 유지한 채 내용만 바꾼다. 레이아웃이 흔들리지 않게 한다.

---

## 5. 요청 카드 (공통 컴포넌트)

노인의 음성/텍스트 요청은 **하나의 카드 모델**로 표현되고, 세 화면이 같은 컴포넌트를 밀도만 바꿔 재사용한다.

카드에 담기는 것:

| 슬롯 | 내용 |
|---|---|
| 상태 배지 | `신규` `진행중` `완료` `반려` — §6 색 규칙 |
| 제목 | 요청 종류 (예: 병원동행) |
| 요약 | AI가 만든 한 문장. AI 배지를 함께 표시 |
| 원문 | 노인이 말한 그대로. 접었다 펼 수 있게 |
| 메타 | 요청 시각, 담당자 |
| 행동 | 역할에 따라 다르게: 노인=`취소`, 복지사=`담당 맡기`·`상태 변경` |

역할별 차이는 **행동 버튼과 메타 노출 범위**뿐이다. 카드 구조·간격·색은 세 화면에서 동일하다.
노인 화면에는 내부 행정 메모를 노출하지 않는다.

---

## 6. 상태 색 매핑

| 상태 | 색 |
|---|---|
| 긴급 / 미확인 | Red |
| 주의 / 대기 / 확인 필요 | Amber |
| 안정 / 완료 / 검증됨 | Mint |
| 신규 / AI 생성 | Blue |
| 정보 없음 / 반려 | Gray (`--bg-alt` + `--ink-2`) |

전 화면에서 이 표를 따른다. `등록됨`(Amber)과 `법적으로 검증됨`(Mint)은 절대 같은 색으로 표시하지 않는다.

---

## 7. 접근성

- 명도 대비 4.5:1 이상. `--ink-3`는 메타 정보에만 쓰고 본문에 쓰지 않는다.
- 색만으로 정보를 전달하지 않는다. 항상 텍스트 라벨을 함께 둔다.
- 200% 확대에서 가로 스크롤이 생기지 않아야 한다.
- 키보드 포커스 링을 제거하지 않는다.
- 상태 변경(새 요청 도착, 음성 재생 시작)은 `aria-live`로 알린다.
- 긴급 화면은 외부 리소스(웹폰트·이미지·아이콘 CDN) 없이 렌더링되어야 한다.

---

## 8. 하지 말 것

- 그림자로 카드 구분하기
- 토큰에 없는 색·간격·글자 크기 사용
- 역할별로 다른 컴포넌트 만들기 (밀도 스케일로 해결)
- 장식용 일러스트·배경 이미지·그라데이션
- 한 화면에 1차 버튼 2개 이상
- 사람이 쓴 내용에 AI 배지 붙이기
- 목업 HTML의 인라인 스타일을 그대로 복사하기 — 토큰으로 옮겨 쓴다

---

## 9. 목업 파일의 위치

`Senior.dc.html`, `Family.dc.html`, `Worker.dc.html`, `index.html`은 **초기 탐색 자료**다.
문구와 정보 구조는 참고하되, 배치·간격·색은 이 문서를 따른다. 둘이 어긋나면 이 문서가 이긴다.
