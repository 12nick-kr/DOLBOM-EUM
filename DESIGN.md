# 돌봄이음 AI — 디자인 명세서 (Codex 구현 참고용)

> 기준 문서: `PRD_돌봄이음_AI.md` v1.1, `CODEX_TERRA_TDD_통합_프롬프트.md`
> 시각 목업: `Senior.dc.html`(노인), `Family.dc.html`(부양가족), `Worker.dc.html`(사회복지사)
> 기존 참고 목업: `index.html` (색상·문구 톤 참고용으로 보존)

이 문서는 3개 역할(`/senior`, `/family`, `/worker`) 화면의 디자인 토큰과 화면별 구성을 정의한다. 실제 구현 시 Next.js 컴포넌트/스타일은 이 문서와 각 `.dc.html` 목업을 함께 참고한다.

---

## 1. 디자인 방향

- 전체 톤: 토스(Toss) 스타일 — 화이트 배경, 파란색 포인트, 넉넉한 여백, 둥근 카드, 절제된 그림자, 명확한 위계.
- 노인·가족 화면은 사회복지사 화면과 **시각 언어(컬러·타이포·카드·라운딩)를 통일**하되, 노인 화면만 접근성 기준으로 크기를 확대한다.
- 사회복지사 화면은 데스크톱 관리자 대시보드 레이아웃(사이드바 + 카드 그리드 + AI 요약 패널)을 사용한다.
- 모든 화면 상단에 `챌린지 데모 — 실제 접수 아님` 배지를 유지한다 (PRD 22장 위험 대응).

## 2. 디자인 토큰

### 2.1 컬러

| 용도 | 값 | 비고 |
|---|---|---|
| Primary Blue | `#3182F6` | 기본 액션, 링크, 활성 상태 |
| Primary Blue Dark | `#1B64DA` | 버튼 hover/눌림 |
| Blue Light (배경) | `#EBF2FE` | AI 태그, 선택된 탭, 강조 배경 |
| Ink (본문) | `#191F28` | 제목, 본문 텍스트 |
| Ink 2 (보조 텍스트) | `#4E5968` | 라벨, 서브텍스트 |
| Ink 3 (설명/placeholder) | `#8B95A1` | 힌트, 메타정보 |
| Line (구분선/테두리) | `#E5E8EB` | 카드 테두리, 구분선 |
| Surface Alt (연한 배경) | `#F2F4F6` | 카드 내부 블록, 비활성 배경 |
| Page Background | `#F9FAFB` / `#F2F4F6` | 전체 배경, 사이드바 배경 |
| Red (긴급/위험) | `#F04452` / 배경 `#FFEDEE` | 긴급 알림, 위기 화면 |
| Amber (주의) | `#FF9F1A` / 배경 `#FFF4E1` | 주의 필요, 등록됨(미검증) 상태 |
| Green/Mint (안정/완료) | `#01C88E` / 배경 `#E3FBF3` | 안정 상태, 완료, 법적 검증됨 |

톤은 최대 1개 브랜드 블루 + 3개 시맨틱 컬러(빨강/주황/민트)로 제한한다. 새로운 색을 추가하지 않는다.

### 2.2 타이포그래피

- 폰트: `Pretendard` (한글), 폴백 `-apple-system, 'Apple SD Gothic Neo', 'Malgun Gothic', sans-serif`.
- 굵기: 본문 600, 제목/버튼 800(bold)을 기본으로 사용해 토스 특유의 굵은 강조를 유지한다.
- 크기 기준(일반 화면 · 노인 화면):
  - 화면 타이틀: 18px · 24~25px
  - 본문/버블 텍스트: 14~16px · 16~22px
  - 보조 텍스트/메타: 12~13px · 15~16px
  - 버튼 텍스트: 14~16px · 17~24px

### 2.3 형태·간격

- 카드 radius: 16~20px (모바일), 16~18px (데스크톱)
- 폰 프레임 radius: 36px, 버튼 radius: 12~16px, 배지/필: 999px(pill)
- 카드 그림자: `0 4px 20px rgba(15,23,42,.06)` (일반), 프레임 자체는 `0 30px 70px rgba(15,23,42,.14~.16)`
- 여백: 카드 내부 패딩 14~22px, 화면 좌우 패딩 22~28px
- 아이콘: 이모지 사용(디자인 시스템 톤에 맞춰 절제), 커스텀 SVG 아이콘 제작 금지 — 실제 구현 시 아이콘 폰트/라이브러리로 대체 가능

## 3. 공통 컴포넌트 패턴

- **상태 배지(Pill)**: 배경 연한 시맨틱 컬러 + 텍스트 진한 시맨틱 컬러, `font-weight:800`, `border-radius:999px`. 예: 긴급(Red), 주의(Amber), 안정/완료(Mint), 정보 없음(Gray), 신규(Blue).
- **AI 생성 표시**: `🤖 AI 요약` / `🤖 AI 생성` / `🤖 AI 초안` 배지를 Blue Light 배경으로 항상 표기 (PRD 16장 요구사항).
- **1차 버튼**: Primary Blue 배경, 흰 텍스트, 800 weight, radius 12~16px.
- **2차/고스트 버튼**: Surface Alt 또는 흰 배경 + Blue 테두리.
- **위험 버튼**: Red 배경(119 전화 등 고위험 액션에만 사용).
- **카드**: 흰 배경 + `1~1.5px` Line 테두리 (그림자는 최소화, 토스 스타일은 테두리 기반 구분을 선호).
- **음성 상태 컨트롤**: `🔊 재생 중` 칩(Blue Light) + `다시 듣기`/`일시정지`/`그만 듣기` 소형 버튼 3종을 항상 세트로 노출 (PRD 7.1, FR-08).

## 4. 화면별 명세

### 4.1 노인 화면 `/senior` — `Senior.dc.html`

접근성 원칙: 버튼 최소 56px 높이(핵심 버튼 60~72px), 본문 16px 이상, 화면당 선택지 최대 2개, 모든 답변에 텍스트+음성 상태를 함께 표시.

| # | 화면 | 핵심 요소 | 관련 요구사항 |
|---|---|---|---|
| 1 | 홈 | 인사말, 212px 원형 말하기 버튼, 고정 긴급 도움 버튼(우하단), 3분할 보조 액션(음식 찍기/내 요청/복지 정보), 하단 내비(홈·말동무·내 요청) | 7.1 홈 화면, FR-02 |
| 2 | 음성 듣는 중 | "듣고 있어요" 캡션, 파형 애니메이션, 녹음 중 배지, 취소 버튼 | 7.1 음성 요청 |
| 3 | 발화 확인 | "제가 이렇게 들었어요" + 인용 카드, [다시 말할게요]/[맞아요] 2택 | 7.1 음성 요청 |
| 4 | AI 안내(요청 흐름) | 대화 버블(AI/사용자), 요청 요약 카드, 음성 컨트롤 3종, [취소]/[보내주세요] | FR-05, FR-08 |
| 5 | 긴급 도움 | 전체 레드 배경, 감지 요약(위치·발화·시각·주의사항), 119 전화(1차)·가족 알림·복지사 알림(2차) 버튼, 취소 링크 | FR-03, 7.1 긴급 도움 |
| 6 | 음식 사진 촬영 | 카메라 박스(점선 테두리), [사진 촬영하기]/[앨범에서 선택] | FR-04 |
| 7 | 음식 분석 결과 | 결과 카드(추정 음식/재료/확신도/충돌 가능성), AI 안내 버블 + 음성 컨트롤, [알겠어요] | FR-04 |
| 8 | 말동무 | 대화 버블, 하단에 "대화 원문은 가족에게 공유되지 않음" 고지 | 7.1 말동무 |
| 9 | 내 요청 보기 | 상태별(진행중/완료/음식확인) 카드 리스트 | 7.1 홈 보조 기능 |

색상: 홈·정보 화면은 Blue 톤, 긴급 화면만 전면 Red. 텍스트는 항상 Ink(#191F28) 이상 대비를 유지.

### 4.2 부양가족 화면 `/family` — `Family.dc.html`

| # | 화면 | 핵심 요소 | 관련 요구사항 |
|---|---|---|---|
| 1 | 상태 대시보드 | 긴급 알림 카드(있을 때만 상단 고정), 마지막 확인/체크인 통계, 7일 변화 요약, AI 요약(출처 링크 포함), "확인되지 않음" 상태 표기 | 7.2 상태 요약 대시보드 |
| 2 | 위기 알림 상세 | 감지 정보(시각/위치/발화/근거), 처리 타임라인, [전화]/[복지사 공유]/[확인 완료] | 7.2 위기 알림 상세, FR-06 |
| 3 | 동의·대리·결정 권한 | 항목별 동의 토글(건강/위치/위기/대화원문), 대리 문서 카드(등록됨=Amber, 법적 검증됨=Mint로 구분), 위기 결정 요청 카드 | 7.2 동의·대리·결정 권한 |

주의: "등록됨"과 "법적으로 검증됨"은 색과 라벨을 다르게 하여 절대 동일하게 표시하지 않는다.

### 4.3 사회복지사 화면 `/worker` — `Worker.dc.html`

데스크톱 관리자 레이아웃: 좌측 사이드바(224px, 로고+메뉴 5종+소속 기관 정보) + 본문. 사이드바 메뉴: 대시보드, 위기 알림, 요청 업무함, 사례 관리, 설정.

| # | 화면 | 핵심 요소 | 관련 요구사항 |
|---|---|---|---|
| 1 | 담당자 대시보드 | 상단 인사말+알림+프로필, 4분할 상태 통계(긴급/주의/안정/신규), 긴급 케이스 하이라이트 카드, 7일 요청 추이 바 차트, 담당 노인 목록(상태 배지), 우측 레일(오늘 할 일 + AI 요약) | 7.3 담당자 대시보드 |
| 2 | 노인 사례 상세 | 프로필 헤더(상태 배지 포함), 기본 정보 카드, 작성자/AI생성 여부가 표시된 타임라인, 우측 긴급 대응 액션 + AI 행정 보조 패널 | 7.3 노인 사례 상세 |
| 3 | 요청 업무함 | 상태 필터 탭(전체/신규/진행중/완료), 요청 카드 리스트(상태 배지 + 담당자/기한 메타) | 7.3 요청 업무함 |
| 4 | 병원동행 요청 상세 | 요청 정보 카드, AI 신청 초안(출처·기준일 명시), 담당자 메모 입력, [정보 확인 요청]/[기관 연결로 승인] | 7.3 요청 업무함, FR-05 |

바 차트는 SVG 대신 단순 CSS 막대(높이 %)로 구현되어 있으며, 실 데이터 연동 시에도 동일한 방식(라이브러리 없는 경량 바 차트)을 권장한다.

## 5. 상태·색상 매핑 규칙 (전 화면 공통)

| 상태 | 색상 | 사용처 |
|---|---|---|
| 긴급 / 미확인 | Red | 긴급 알림, 긴급 케이스 배지 |
| 주의 / 가족 확인 대기 / 정보 확인 필요 | Amber | 주의 필요 케이스, 서비스 요청 중간 상태 |
| 안정 / 완료 / 법적으로 검증됨 | Mint | 정상 상태, 완료된 요청, 검증된 문서 |
| 정보 부족 / 반려 / 확인되지 않음 | Gray(#F2F4F6 배경 · #4E5968 텍스트) | 데이터 없음, 반려된 요청 |
| 신규 / AI 생성·요약 | Blue | 신규 요청, AI 산출물 태그 |

## 6. 구현 시 유의사항 (PRD/TDD 프롬프트 연계)

- 모든 노인 화면의 AI 답변에는 텍스트와 음성 컨트롤(다시 듣기/일시정지/그만 듣기)이 함께 있어야 한다 — 목업의 버튼 3종 세트를 그대로 컴포넌트화할 것.
- 긴급 화면(노인)과 위기 알림 상세(가족)는 실제 서버/AI 장애 시에도 항상 렌더링 가능해야 하므로, 정적 텍스트·아이콘만으로 구성했다(외부 리소스 의존 없음).
- "AI 요약", "AI 생성", "AI 초안" 배지는 실제로 AI가 생성한 콘텐츠에만 붙이고, 사람이 작성/승인한 내용과 시각적으로 구분되게 유지한다.
- 사회복지사 화면의 소득기준 확인 필요, 기준일 등 문구는 실제 데이터 연동 전까지 목업 문구를 유지하고, 실제 구현 시 정책 카드에는 출처 링크를 실제 URL로 연결한다.
- 색상·폰트·라운딩 값은 위 토큰표를 그대로 CSS 변수/테마 설정에 반영해 3개 역할 화면 간 일관성을 유지한다.
