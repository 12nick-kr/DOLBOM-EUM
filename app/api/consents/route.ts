import { NextRequest, NextResponse } from 'next/server'; import { z } from 'zod'; import { demoFamilyId, demoSeniorId, id, state } from '@/lib/server/store';
export async function POST(request: NextRequest) { const data = z.object({ scope: z.enum(['health', 'location', 'service', 'emergency', 'conversation_summary']), expiresAt: z.string() }).safeParse(await request.json()); if (!data.success) return NextResponse.json({ error: '동의 항목을 확인해 주세요.' }, { status: 400 }); const consent = { id: id('consent'), seniorId: demoSeniorId, granteeId: demoFamilyId, ...data.data, revokedAt: null }; state.consents.push(consent); return NextResponse.json(consent, { status: 201 }); }
