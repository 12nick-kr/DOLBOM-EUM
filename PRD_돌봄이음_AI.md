# 돌봄이음 AI — 제품 요구사항 정의서(PRD)

> 작업명: **돌봄이음 AI**  
> 문서 버전: v1.2  
> 작성 기준일: 2026-08-25  
> 프로젝트 기간: 3일, 매일 10:00–18:00  
> 대상: 노인, 부양가족, 사회복지사
> 외부 연동 검증 방식: 환경변수의 값이나 실제 API를 호출하지 않고 공식 문서와 환경변수 **이름**만 확인

### v1.2 변경 요약

- **음식 사진 분석 기능을 전면 제거했다.** 3일 MVP 대비 이미지 파이프라인의 리소스 소모와 운영·품질 관리 부담이 크다고 판단해, 노인 화면의 촬영·업로드 경로, `/api/ai/food`, FR, 관련 시나리오·테스트·위험 항목을 모두 삭제했다. 이에 따라 노인의 입력 방식은 **음성과 텍스트 두 가지**이며, 핵심 수직 시나리오는 **위기 대응과 복지 요청 두 개**로 축소된다.
- **AI 호출 자격증명을 `OPENAI_API_KEY`와 `OPENAI_PROJECT_ID` 두 개로 통합했다.** 공식 문서 기준으로 Responses(`gpt-5.6-terra`), Transcription(`gpt-transcribe`), Speech(`gpt-4o-mini-tts`) 세 엔드포인트는 모두 동일한 API 키 하나로 인증하며 모델은 요청마다 `model` 필드로 선택하므로, 이 구성으로 PRD의 모든 모델을 호출할 수 있다. 두 키는 AI 기능에만 사용하고 Supabase·공공데이터·Kakao 키와 통합하지 않는다.
- 다만 키의 존재가 모델 권한을 보장하지 않는다. 프로젝트의 모델 허용 목록, 결제·사용 등급, API 키의 restricted 스코프에 따라 실패할 수 있으므로 판정은 **`조건부·미검증`**을 유지하며, §14.2 smoke test로만 `실행 확인`으로 승격한다.
- 이번 개정에서도 실제 API 호출은 하지 않았고 공식 문서와 환경변수 **이름**만 확인했다.

<details>
<summary>v1.1 변경 요약 (이력)</summary>

- 현재 `.env`의 `OPENAI_API_KEY`, `OPENAI_PROJECT_ID`가 각각 인증과 프로젝트 범위 지정에 쓰이며, 키 자체가 모델 하나를 고정하지 않는다는 점을 명시했다.
- 노인의 입력 방식이 음성·텍스트·사진 중 무엇이든 모든 챗봇 답변을 **화면 텍스트와 음성으로 동시에** 제공하도록 P0 요구사항과 인수 조건을 추가했다.
- 2026-08-24 공식 문서 기준으로 외부 API의 기능, 문서상 제공 상태, 현재 환경에서의 실행 준비 상태를 분리해 판정했다. 인증 호출은 하지 않았으므로 계정별 모델 권한과 실제 응답 성공은 미검증이다.
- 가족관계증명서 같은 외부 증빙은 비공개 Object Storage 원본과 DB 메타데이터로, 앱에서 생성한 동의·위임은 구조화 레코드와 버전 고정 PDF 증적으로 저장하는 혼합 전략을 확정했다.
- Supabase Free 한도와 민감문서 특성을 고려해 실제 문서 대신 합성 데모 문서만 사용하고, 비공개 버킷·RLS·짧은 서명 URL·보관기한·감사 로그를 필수화했다.

</details>

---

## 1. 한 줄 정의

노인이 **말하거나 짧게 입력하는 것만으로** 도움을 요청하면, AI가 의도를 이해하고 필요한 정보·신청 초안·위기 알림을 만들며, 가족과 사회복지사가 같은 상황을 권한에 맞게 함께 처리하는 돌봄 협업 웹 서비스다.

## 2. 제품 비전과 핵심 가치

현재 돌봄 서비스는 정보가 여러 기관에 흩어져 있고, 노인은 복잡한 검색과 신청 절차를 어려워하며, 가족과 사회복지사는 서로 다른 경로로 상황을 확인한다. 돌봄이음 AI는 다음의 한 흐름을 만든다.

**노인의 자연어 요청 → AI의 의도·위험도 파악 → 적합한 정보 또는 업무 생성 → 본인/담당자 최종 확인 → 가족·사회복지사에게 필요한 범위만 공유**

핵심 가치는 다음 세 가지다.

1. **쉬운 접근:** 노인은 복잡한 메뉴 대신 말, 짧은 텍스트, 큰 버튼으로 이용한다.
2. **끊기지 않는 돌봄:** 요청이 정보 제공에서 끝나지 않고 가족·사회복지사의 실제 처리 업무로 이어진다.
3. **안전한 협업:** 건강·위치·의사결정 정보는 동의와 역할별 권한에 따라 최소한으로 공유한다.

## 3. 문제 정의

### 3.1 노인

- 검색어 입력, 복잡한 메뉴, 긴 신청서 작성이 어렵다.
- 자신의 증상이 긴급한지, 어떤 기관이나 복지서비스를 찾아야 하는지 판단하기 어렵다.
- 독거노인은 일상 대화 상대와 도움 요청 통로가 부족하다.

### 3.2 부양가족

- 멀리 떨어져 있을 때 노인의 현재 상태와 최근 변화가 보이지 않는다.
- 모든 대화와 건강정보를 보는 것은 사생활 침해가 될 수 있지만, 위기 때는 필요한 정보를 빨리 알아야 한다.
- 위임·동의 문서와 긴급상황 결정 요청이 전화, 메시지, 종이 문서로 흩어져 있다.

### 3.3 사회복지사

- 여러 노인의 상태, 배경, 상담 기록, 서비스 신청 진행 상황을 동시에 관리해야 한다.
- 반복적인 상담 요약, 자격 확인, 기관 탐색, 신청서 초안 작성에 시간이 많이 든다.
- 긴급상황 발생 시 가족과 최신 정보를 빠르게 공유하기 어렵다.

## 4. 목표와 비목표

### 4.1 3일 MVP 목표

- 세 역할에 맞는 독립 화면과 권한을 제공한다.
- 노인이 음성 또는 텍스트로 요청할 수 있다.
- 노인이 음성과 텍스트 중 어떤 방식으로 요청하더라도 AI 답변의 텍스트 표시와 한국어 음성 재생을 함께 제공한다.
- AI가 요청을 `일상`, `복지`, `건강 주의`, `긴급`으로 분류하고 다음 행동을 제안한다.
- 노인의 요청이 가족·사회복지사 화면에 실시간으로 전달되는 하나의 완성된 수직 흐름을 구현한다.
- 병원 및 충남 노인복지시설을 실제 공개 데이터로 검색한다.
- 고위험 행동은 AI가 단독 실행하지 않고 사용자 또는 담당자의 명시적 확인을 받는다.

### 4.2 MVP 비목표

- 의학적 진단, 처방, 약물 변경을 하지 않는다.
- 음식 사진 분석과 식이 안전 판단은 이번 MVP 범위에서 제외한다.
- 치매 여부나 법적 의사결정 능력을 판정하지 않는다.
- 119 신고 접수, 정부 민원, 병원동행 서비스 예약이 실제 기관 시스템에서 완료됐다고 표시하지 않는다.
- 웨어러블·병원 EMR·장기요양기관 시스템을 실제 연동하지 않는다.
- AI 대화가 전문 의료·정신건강 상담이나 사람의 돌봄을 대체하지 않는다.

## 5. 핵심 제품 전략: 하나의 수직 시나리오

기능을 넓게 만들기보다 아래 두 흐름을 끝까지 연결하는 것이 챌린지 데모의 핵심이다.

1. **위기 대응:** 노인 “가슴이 아프고 숨이 차” → 긴급 의도 감지 → `119 전화하기`와 위치·증상 요약 제시 → 가족/사회복지사에게 위기 알림 → 처리 상태 공유
2. **복지 요청:** 노인 “다음 주 병원 갈 때 같이 갈 사람이 필요해” → 병원동행 요청 초안 → 노인 확인 → 사회복지사 업무함 → 담당자 승인/처리 → 가족에게 상태 공유

이 두 흐름만 완성해도 음성 AI, 텍스트·음성 동시 답변, 도구 호출, 세 역할 협업, 실시간 알림, 공공데이터 연동을 모두 시연할 수 있다. 기능을 넓히는 대신 두 흐름의 완성도와 안전장치에 남은 시간을 쓴다.

## 6. 사용자와 권한 원칙

| 역할 | 주요 목적 | 기본 조회 범위 | 주요 행동 |
|---|---|---|---|
| 노인 | 쉽고 빠르게 도움 요청 | 본인의 기록·요청·동의 상태 | 음성/텍스트 입력, 요청 확인·취소, 공유 범위 선택 |
| 부양가족 | 동의된 범위 내 상태 확인과 위기 대응 | 연결된 노인의 요약 정보, 위기 시 허용된 추가 정보 | 알림 확인, 연락, 위기 결정 요청 응답 |
| 사회복지사 | 담당 노인의 사례·서비스 관리 | 자신에게 배정되고 동의가 유효한 노인 | 기록 작성, 요청 처리, 서비스 연결, 위기 대응 |

권한은 단순히 “가족이므로 전체 열람”으로 설계하지 않는다. `누가`, `어떤 정보 항목을`, `무슨 목적으로`, `언제까지` 볼 수 있는지 동의 단위로 저장한다.

## 7. 정보 구조와 화면 요구사항

### 7.1 노인 화면 `/senior`

#### 홈 화면

- 상단에 현재 시간과 “○○님, 무엇을 도와드릴까요?” 문구
- 화면 중앙의 큰 **말하기** 버튼
- 항상 같은 위치에 고정된 빨간 **긴급 도움** 버튼
- 보조 기능 2개: `내 요청 보기`, `복지 정보`
- AI 답변은 짧은 문장으로 화면 표시와 음성 읽기를 동시에 제공
- 입력 방식과 무관하게 AI 답변은 짧은 문장으로 화면 표시와 음성 읽기를 동시에 제공
- 한 화면에는 하나의 핵심 질문과 최대 2개의 선택지만 노출

#### 음성 요청

- 버튼을 누르고 말하면 음성 파형과 “듣고 있어요” 표시
- 발화를 텍스트로 보여주고 “제가 이렇게 들었어요”라고 다시 확인
- AI가 구조화된 의도, 위험도, 필요한 정보, 제안 행동을 반환
- 일반 정보는 바로 안내하고, 신청·연락·공유는 최종 확인 후 실행
- 사용자가 “취소”, “다시 말할게”라고 말해도 동작

#### 챗봇 답변 음성 출력

- 노인이 음성, 키보드 텍스트, 선택 버튼 중 어느 입력을 사용하더라도 `assistant_text`와 동일한 내용의 한국어 음성을 자동 재생한다.
- 브라우저의 자동 재생 제한을 피하기 위해 첫 화면의 `대화 시작` 또는 `말하기` 터치에서 오디오 사용을 활성화한다. 그 전에는 텍스트를 먼저 표시하고 `답변 듣기` 버튼을 제공한다.
- 모든 답변에 `다시 듣기`, `일시정지/계속`, `그만 듣기`를 제공하고 음성 속도 기본값은 고령자 테스트로 조정한다.
- 새 답변을 재생하기 전에 이전 음성을 중단해 두 음성이 겹치지 않게 한다. 사용자가 다시 말하기를 시작하면 현재 TTS도 즉시 중단한다.
- 서버 TTS가 실패하면 브라우저 `speechSynthesis`의 한국어 음성으로 1회 폴백한다. 이마저 불가능하면 텍스트와 명확한 오류 상태를 유지하며, 긴급 버튼은 영향을 받지 않는다.
- 긴급 안내의 핵심 문장은 네트워크·AI 장애와 무관하게 재생할 수 있는 앱 내 고정 문구 또는 사전 준비 음원을 사용한다.
- 사용자가 `음성 답변 끄기`를 선택할 수 있고 선호는 `accessibility_preferences`에 저장한다. 단, 화면 텍스트는 항상 제공한다.

#### 긴급 도움

- 위험 표현을 감지하면 대화 UI를 즉시 중단하고 긴급 화면으로 전환
- 큰 버튼: `119 전화하기`, `가족에게 알리기`, `사회복지사에게 알리기`
- 현재 위치 권한이 있으면 위치, 발화 원문, 감지 시각, 최근 등록된 중요 주의사항을 요약
- 전화 연결 전 “지금 119에 전화할까요?”를 크게 읽고 한 번 확인
- 반응이 없거나 웹이 종료된 경우 자동 신고가 된 것처럼 표시하지 않음

#### 말동무

- 5분 내외의 짧은 일상 대화, 회상 질문, 오늘의 일정 안내
- 외로움·죽고 싶음·학대 등 고위험 표현은 일반 대화에서 분리해 안전 흐름으로 전환
- 가족에게 전체 대화 내용을 공유하지 않고, 노인이 동의한 안부 요약만 공유

### 7.2 부양가족 화면 `/family`

#### 상태 요약 대시보드

- 오늘의 상태: 마지막 확인 시각, 노인의 직접 체크인, 식사/약 복용의 사용자 입력 상태
- 최근 7일 변화: 요청 수, 위기 알림, 미처리 서비스 요청
- AI가 만든 요약에는 반드시 “AI 요약” 표시와 원본 기록 링크 제공
- 수집되지 않은 정보는 정상으로 추정하지 않고 “확인되지 않음”으로 표시

#### 위기 알림 상세

- 감지 시각, 위치, 노인의 발화 원문, 위험도 근거, 현재 처리자와 처리 상태
- `노인에게 전화`, `119 전화`, `사회복지사와 공유`, `확인 완료` 행동
- 가족이 확인하면 사회복지사 화면에 즉시 상태 반영

#### 동의·대리·결정 권한

- 동의 범위: 건강 요약, 위치, 서비스 요청, 위기 정보 등 항목별 표시
- 대리 문서: 문서 유형, 업로드 파일, 유효기간, 검토 상태
- `등록됨`과 `법적으로 검증됨`을 구분
- 위기 결정 요청: 상황, 선택지, 요청자, 기한, 근거자료, 응답 기록
- 치매 진단만으로 가족에게 모든 결정권을 자동 부여하지 않음

### 7.3 사회복지사 화면 `/worker`

#### 담당자 대시보드

- 담당 노인을 `긴급`, `주의`, `안정`, `정보 부족`으로 분류
- 정렬 기준: 미확인 긴급 알림 → 기한 임박 요청 → 최근 상태 악화 → 일반 업무
- 카드에는 최소 정보만 표시하고 상세 화면에서 권한에 따라 추가 확인

#### 노인 사례 상세

- 기본 배경, 건강 주의사항, 가족 연락처, 동의 범위, 최근 상호작용, 서비스 이력
- 타임라인에 작성자와 원본/AI 생성 여부를 명시
- 중요한 정보 변경 이력과 열람 기록 유지

#### AI 행정 보조

- 상담 기록 요약
- 노인의 요구에서 필요한 서비스 후보 추출
- 자격요건 체크리스트와 누락 정보 질문 생성
- 신청서·상담일지·가족 안내문 초안 작성
- 모든 결과는 사회복지사가 수정·승인한 후 저장 또는 발송
- 정책 정보에는 출처, 기준일, 지역, 확인 필요 여부 표시

#### 요청 업무함

- 상태: `신규`, `정보 확인`, `가족 확인`, `기관 연결`, `완료`, `반려`
- 담당자 지정, 기한, 메모, 다음 행동
- 노인에게는 내부 행정 메모가 아니라 쉬운 상태 문구만 노출

## 8. 역할별 MVP 우선순위

| 우선순위 | 노인 | 부양가족 | 사회복지사 |
|---|---|---|---|
| P0 | 큰 음성 버튼, 텍스트 대체 입력, 모든 AI 답변 음성 읽기, 긴급 도움, 요청 확인 | 상태 요약, 위기 알림, 연락 버튼 | 담당자 목록, 위기 알림, 요청 업무함, 사례 상세 |
| P1 | 복지정보 추천, 말동무 | 동의 범위, 요청 진행 상태 | AI 상담 요약, 서비스 추천·신청 초안 |
| P2 | 일정·복약 알림, 방언 적응 | 위임 문서 갱신 알림 | 기관 시스템 연동, 대량 보고서, 일정 자동화 |

3일 MVP의 완료 기준은 P0 전체와 P1 중 `복지정보 추천`, `동의 범위`, `AI 신청 초안`까지다.

## 9. 기능 요구사항과 인수 조건

### FR-01 역할 기반 로그인

- 사용자는 노인, 가족, 사회복지사 역할로 로그인한다.
- 데모에서는 역할별 테스트 계정을 제공한다.
- 다른 관계의 노인 데이터 URL을 직접 입력해도 조회되지 않아야 한다.

### FR-02 음성 의도 파악

- 20개의 사전 정의 한국어 발화에서 의도 분류 정확도 85% 이상을 목표로 한다.
- 결과는 `intent`, `urgency`, `summary`, `missing_fields`, `proposed_tool`, `requires_confirmation` 구조로 반환한다.
- 확신이 낮으면 임의 실행하지 않고 한 가지 추가 질문을 한다.

### FR-03 고위험 발화 처리

- 가슴 통증, 호흡 곤란, 의식 저하, 심한 출혈, 자해 위험 등 테스트 문구를 긴급으로 분류한다.
- 긴급 화면은 1초 이내 표시되고 119 전화 버튼은 한 번의 조작으로 연결된다.
- 가족·사회복지사 알림 생성과 처리 상태 변경이 감사 로그에 남는다.

### FR-04 서비스 요청

- 음성에서 서비스 종류, 희망 일시, 목적지, 이동 지원 필요 여부를 추출한다.
- 누락 필드는 한 번에 하나씩 질문한다.
- 노인의 최종 확인 후 사회복지사 업무함에 `신규` 요청을 생성한다.

### FR-05 실시간 협업

- 긴급 알림과 요청 상태 변경이 연결된 화면에 3초 이내 반영되는 것을 목표로 한다.
- 알림을 확인한 사람과 시각을 표시한다.

### FR-06 근거 있는 정책 안내

- 정책 카드에 정책명, 대상 조건, 지역, 신청 방법, 출처 링크, 마지막 확인일을 표시한다.
- 출처가 없거나 기준일이 오래된 정보는 자동 신청 대상으로 제안하지 않는다.

### FR-07 텍스트·음성 동시 답변

- 노인용 챗봇의 모든 성공 응답은 입력 종류와 관계없이 `assistant_text`를 먼저 화면에 표시하고 같은 의미의 음성을 재생한다.
- 서버는 임의 클라이언트 문자열이 아니라 권한이 확인된 `assistant_turn_id`의 답변만 TTS로 변환한다.
- 음성 생성 목표는 답변 텍스트 확정 후 2초 안에 재생 시작이다. 5초를 넘기면 브라우저 TTS 폴백을 시도한다.
- 음성 파일은 기본적으로 영구 저장하지 않고 스트리밍하거나 짧은 메모리/서버 캐시만 사용하며 응답에 `Cache-Control: private, no-store`를 적용한다.
- TTS 실패, 브라우저 음성 미지원, 자동 재생 차단, 네트워크 중단 각각에 대해 텍스트 대체 경로와 `다시 듣기`가 동작해야 한다.
- AI 음성임을 최초 대화 시작 시 알리고, 사람 또는 기관 상담원의 실제 음성으로 오인하게 만들지 않는다.

## 10. AI 동작 정책

### 10.1 AI가 할 수 있는 일

- 음성·텍스트 요청을 구조화
- 정책·시설 데이터에서 조건에 맞는 후보 검색
- 상담 기록·위기 상황 요약
- 신청서와 안내문 초안 작성
- 승인된 답변 텍스트를 노인 친화적인 한국어 음성으로 변환
- 허용된 서버 함수를 선택해 호출 요청

### 10.2 AI가 단독으로 하면 안 되는 일

- 119 신고가 접수됐다고 선언
- 의료 진단·처방·복약 변경
- 법적 의사능력이나 대리권 확정
- 정부 민원 또는 유료 서비스의 최종 제출·결제
- 가족·사회복지사에게 동의 범위를 넘은 원문 대화 공개
- 가족관계증명서·위임장 원본을 법적 효력이 검증된 문서라고 판정하거나, MVP에서 해당 원본을 AI/OCR 서비스로 전송

### 10.3 도구 호출 안전 등급

| 등급 | 예시 | 실행 조건 |
|---|---|---|
| 낮음 | 정책·시설 검색, 내 요청 조회 | 즉시 실행 가능 |
| 중간 | 서비스 요청 생성, 가족에게 안부 공유 | 사용자에게 읽어주고 확인 후 실행 |
| 높음 | 긴급 연락, 민감정보 공유, 대리 결정 요청 | 명시적 확인, 서버 권한 재검증, 감사 로그 필수 |

## 11. 추천 기술 구조

사용자가 제안한 React + Next.js + Supabase를 다음처럼 정리한다. Next.js 자체가 React 프레임워크이므로 프론트와 백엔드를 별도 프로젝트로 나누지 않고 **하나의 Next.js App Router 프로젝트**로 구현하는 것이 3일 일정에 가장 적합하다.

- **프론트엔드:** Next.js App Router의 React Client Components, TypeScript, 모바일 우선 PWA
- **백엔드/BFF:** Next.js Route Handlers (`app/api/**/route.ts`)
- **데이터·인증:** Supabase Postgres, Auth, Storage, Realtime
- **권한:** Supabase Row Level Security(RLS) + 서버의 추가 권한 검사
- **AI:** OpenAI Responses API + `gpt-5.6-terra`(텍스트·구조화 결과·함수 호출), `gpt-transcribe`(음성 전사), `gpt-4o-mini-tts`(음성 출력). 세 모델 모두 `OPENAI_API_KEY` 하나로 인증한다.
- **음성 기본 구조:** 녹음 후 전사 파이프라인. `gpt-realtime-2.1-mini` 기반 Realtime 음성은 P2 실험 기능으로 분리
- **배포:** Next.js 호스팅 1개 + Supabase 프로젝트 1개

Next.js 공식 문서는 App Router가 최신 React 기능을 사용하는 라우터이며 Route Handlers로 HTTP 엔드포인트를 만들 수 있다고 설명한다. Supabase는 브라우저에서 노출되는 테이블에 RLS를 적용하고 Auth와 결합하도록 안내한다.

### 11.1 요청 흐름

1. 브라우저가 텍스트를 전송하거나, 최대 60초의 음성을 Next.js 서버로 전송한다.
2. 음성이면 서버가 `gpt-transcribe`로 전사하고 원본 오디오는 처리 직후 폐기한다. 사용자가 전사문을 확인·수정할 수 있다.
3. 서버가 로그인 사용자, 역할, 관계, 동의 범위를 검증하고 주민등록번호 등 불필요한 식별정보를 마스킹한다.
4. 고정 긴급 키워드와 부정 표현 규칙을 먼저 평가하고, 이후 `gpt-5.6-terra`가 구조화된 의도·답변·도구 호출 후보를 반환한다.
5. 서버가 JSON Schema, 도구 허용 목록, 위험 등급을 검증하고 필요한 경우 사용자 확인 토큰을 요구한다.
6. 승인된 서버 함수만 공공 API 또는 Supabase에 실행한다. 모델에게 임의 URL, SQL, 서비스 키를 제공하지 않는다.
7. 답변 텍스트를 먼저 화면에 표시한 뒤, 권한이 확인된 답변 ID만 `gpt-4o-mini-tts`로 변환해 재생한다.
8. TTS가 5초 안에 시작하지 못하면 브라우저 `speechSynthesis`로 폴백한다. 긴급 안내는 고정 로컬 문구를 사용한다.
9. Supabase Realtime이 가족·사회복지사 화면에 필요한 상태 변경만 전달한다.

OpenAI API 키, Supabase secret 키, 공공데이터 서비스 키, Kakao REST 키는 브라우저에 넣지 않고 서버 환경변수로만 관리한다. OpenAI Responses 요청은 민감정보 최소화 후 `store: false`를 기본으로 한다. 다만 공식 데이터 제어 문서상 API 데이터는 기본적으로 모델 학습에 사용되지 않더라도 일부 엔드포인트의 악용 모니터링 로그가 최대 30일 보관될 수 있으므로, 실제 건강정보를 처리하기 전 보유정책·국외 이전·ZDR 적격 여부를 별도로 검토한다.

### 11.2 AI 자격증명 단일화와 모델 역할

**AI 기능의 외부 호출은 아래 두 환경변수만 사용한다.** 값은 확인하거나 호출하지 않았으므로 키 유효성, 결제 상태, 프로젝트 모델 허용 목록, 실제 호출 성공 여부는 판정하지 않는다.

| 환경변수 | 현재 존재 | 역할 | 모델과의 관계 |
|---|---:|---|---|
| `OPENAI_API_KEY` | 예 | OpenAI 서버 요청 인증. 브라우저 노출 금지 | 하나의 모델 전용 키가 아니다. 코드가 요청마다 `model` 필드로 모델 ID를 선택하며, 프로젝트의 모델 권한·사용 등급·결제 상태에 따라 실제 사용 가능 모델이 달라진다. |
| `OPENAI_PROJECT_ID` | 예 | `OpenAI-Project` 헤더(SDK의 `project` 옵션)로 전달해 사용량·권한 범위를 구분 | 모델명이 아니다. 프로젝트에서 모델 허용/차단과 한도를 설정할 수 있으므로 접근 가능성을 간접적으로 제한한다. 프로젝트 전용 키(`sk-proj-...`)만 쓰는 구성에서는 중복 정보에 가깝지만 사용량 분리를 위해 명시적으로 둔다. |

**두 키로 PRD의 모든 모델을 호출할 수 있는가: 구조상 가능하다.** 근거와 남은 조건은 다음과 같다.

- 이 앱이 쓰는 세 엔드포인트(Responses, Transcription, Speech)는 **동일한 `OPENAI_API_KEY` 한 개**로 인증한다. 엔드포인트나 모델별로 다른 키를 발급받을 필요가 없다.
- 모델은 키가 아니라 **요청 body의 `model` 필드**가 결정한다. 따라서 아래 라우팅 표의 세 모델은 같은 키·같은 프로젝트 헤더로 호출된다.
- 두 키는 **AI 기능 전용**이다. Supabase, 공공데이터포털, Kakao는 각각 별도 키 체계이므로 이 두 변수로 통합할 수 없고 통합하지도 않는다(§11.3).
- 남은 위험은 키 자체가 아니라 **계정 설정**이다. ①프로젝트의 `Limits/Model Usage` 허용 목록, ②결제 등록과 사용 등급(세 모델 모두 Free tier 미지원 표시), ③API 키가 restricted 스코프로 발급되어 해당 엔드포인트 권한이 빠진 경우. 이 셋은 문서로 판정할 수 없으므로 §14.2 smoke test에서만 해소되며, 그전까지 판정은 `조건부·미검증`을 유지한다.

2026-08-24 공식 모델 카탈로그 기준 애플리케이션 권장 라우팅은 다음과 같다. **Codex에서 Terra를 개발 도구로 선택하는 것**과 **완성된 앱이 API 요청에 `gpt-5.6-terra`를 지정하는 것**은 별개 설정이다.

| 작업 | 권장 모델 ID | 선택 이유 | 주의사항 |
|---|---|---|---|
| 챗봇, 의도 구조화, 함수 호출 | `gpt-5.6-terra` | 텍스트 입력·출력, Structured Outputs, function calling을 지원하며 Sol보다 비용 균형형 | 이미지 입력도 지원하는 모델이지만 이번 MVP는 이미지 경로를 사용하지 않는다. 오디오 입출력 모델이 아니다. 공식 표상 Free tier 미지원이므로 프로젝트 결제·권한을 대시보드에서 확인해야 한다. |
| 한국어 녹음 전사 | `gpt-transcribe` | 완료된 음성 파일과 Realtime 세션 입력을 지원하는 고정확도 전사 모델 | 실제 고령자 발화·사투리 평가는 별도 데이터셋으로 수행한다. Free tier 미지원으로 표시되어 있다. |
| 챗봇 답변 음성 합성 | `gpt-4o-mini-tts` | Speech endpoint에서 텍스트를 자연스러운 음성으로 변환 | 최대 입력 길이 제한을 고려해 답변을 짧게 유지한다. AI 음성임을 고지하고 브라우저 TTS 폴백을 둔다. Free tier 미지원으로 표시되어 있다. |
| 선택형 실시간 음성 대화 | `gpt-realtime-2.1-mini` | WebRTC/WebSocket에서 오디오·텍스트 입출력과 함수 호출 지원 | Structured Outputs 미지원이므로 긴급·권한 도구의 최종 판단 경로로 쓰지 않는다. MVP 기본 경로에서 제외한다. |

애플리케이션 코드에 모델명을 흩어 쓰지 않고 아래 비밀이 아닌 설정을 추가한다. 배포 전에 해당 모델이 프로젝트에서 허용되는지는 OpenAI 대시보드의 프로젝트 `Limits/Model Usage`에서 사람이 확인한다.

```dotenv
# 인증 — AI 기능 전용, 서버 전용, 비밀
OPENAI_API_KEY=sk-...
OPENAI_PROJECT_ID=proj_...

# 모델 라우팅 — 비밀 아님
OPENAI_TEXT_MODEL=gpt-5.6-terra
OPENAI_TRANSCRIBE_MODEL=gpt-transcribe
OPENAI_TTS_MODEL=gpt-4o-mini-tts
# OPENAI_REALTIME_MODEL=gpt-realtime-2.1-mini  # P2 실험 전용, MVP 미사용
```

현재 `.env.example`에는 `OPENAI_MODEL`이라는 단일 모델 변수가 남아 있고 값도 위 라우팅과 다른 모델을 가리킨다. 모델을 용도별로 분리한 위 구성과 충돌하므로 구현 착수 시 해당 줄을 제거하고 이 블록의 이름 체계로 교체한다. `.env.example`에는 실제 키 값과 실제 프로젝트 ID를 넣지 않는다.

### 11.3 현재 빠진 필수 연동 설정

AI 자격증명은 §11.2의 두 변수로 통합했지만, **AI 이외의 연동은 각각 별도 키 체계이므로 통합 대상이 아니다.** 현재 `.env`와 `.env.example`에는 아래 이름이 없으므로 OpenAI 이외 연동은 현재 상태 그대로 실행할 수 없다. 새 Supabase 프로젝트는 2026년 권장 키 체계인 publishable/secret 키를 사용하고, RLS를 우회하는 `SUPABASE_SECRET_KEY`는 필요한 서버 관리 작업에만 제한한다.

```dotenv
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
DATA_GO_KR_SERVICE_KEY=
KAKAO_REST_API_KEY=
```

`SUPABASE_SECRET_KEY`, `DATA_GO_KR_SERVICE_KEY`, `KAKAO_REST_API_KEY`, `OPENAI_API_KEY`는 서버 전용이다. `.env*` 실제 파일은 Git에 커밋하지 않고 `.env.example`에는 빈 값과 설명만 둔다.

## 12. 서버 API 초안

| 메서드 | 경로 | 용도 |
|---|---|---|
| POST | `/api/ai/transcribe` | `multipart/form-data`의 짧은 음성을 텍스트로 전사; 원본 미보관 |
| POST | `/api/ai/respond` | 음성 전사문 또는 직접 입력 텍스트를 답변·의도·위험도·도구 후보로 변환 |
| POST | `/api/ai/speech` | 권한이 확인된 `assistant_turn_id`의 답변을 한국어 음성 스트림으로 변환 |
| POST | `/api/ai/intent` | 평가·회귀 테스트용으로 텍스트를 구조화된 의도와 위험도로만 변환 |
| POST | `/api/emergencies` | 위기 이벤트 초안/확정 생성 |
| PATCH | `/api/emergencies/:id` | 확인·연락·종료 상태 갱신 |
| POST | `/api/service-requests` | 서비스 요청 생성 |
| PATCH | `/api/service-requests/:id` | 담당자·상태·메모 갱신 |
| GET | `/api/facilities/hospitals` | 병·의원 검색 프록시 |
| GET | `/api/facilities/welfare` | 충남 노인복지시설 검색 프록시 |
| GET | `/api/policies/recommendations` | 조건 기반 정책 추천 |
| POST | `/api/consents` | 정보 공유 동의 생성·철회 |
| PATCH | `/api/consents/:id/revoke` | 동의를 철회하고 접근권한을 즉시 무효화 |
| POST | `/api/documents` | 허용 형식·크기·권한을 검증해 비공개 문서 업로드 |
| GET | `/api/documents/:id/download` | 매 요청 권한 재검증 후 민감문서를 서버에서 인증 스트리밍 |
| DELETE | `/api/documents/:id` | 보관정책과 legal hold를 확인한 뒤 원본·파생본 삭제 요청 |

AI 함수는 `search_hospital`, `search_welfare_facility`, `draft_service_request`, `create_emergency_draft`, `notify_care_team`처럼 좁고 명확하게 정의한다. AI가 임의 URL이나 SQL을 실행하게 하지 않는다.

`/api/ai/respond` 성공 응답은 최소한 `assistant_turn_id`, `assistant_text`, `intent`, `urgency`, `missing_fields`, `proposed_tool`, `requires_confirmation`, `speech_status`를 포함한다. `/api/ai/speech`는 클라이언트가 보낸 임의 텍스트를 그대로 읽지 않고 DB의 해당 답변을 다시 조회하며, 응답은 `audio/mpeg` 또는 지원되는 스트리밍 형식과 `Cache-Control: private, no-store`를 사용한다.

## 13. 데이터 모델 초안

| 테이블 | 핵심 필드 |
|---|---|
| `profiles` | `id`, `role`, `name`, `phone`, `accessibility_preferences` |
| `care_relationships` | `senior_id`, `member_id`, `relationship_type`, `status`, `starts_at`, `ends_at` |
| `consent_grants` | `senior_id`, `grantee_id`, `scope`, `purpose`, `expires_at`, `revoked_at` |
| `senior_health_profiles` | `senior_id`, `cautions`, `allergies`, `conditions`, `updated_by`, `verified_at` |
| `observations` | `senior_id`, `type`, `value`, `source`, `recorded_at` |
| `service_requests` | `senior_id`, `type`, `details`, `status`, `assignee_id`, `due_at` |
| `emergency_events` | `senior_id`, `level`, `utterance`, `location`, `status`, `created_at` |
| `emergency_actions` | `event_id`, `actor_id`, `action`, `result`, `created_at` |
| `authority_documents` | `id`, `senior_id`, `holder_id`, `type`, `bucket`, `object_path`, `mime_type`, `size_bytes`, `sha256`, `review_status`, `reviewer_id`, `issued_at`, `expires_at`, `retention_until`, `deleted_at` |
| `consent_artifacts` | `consent_grant_id`, `version`, `structured_payload`, `content_hash`, `signed_at`, `revoked_at`, `pdf_object_path` |
| `document_access_logs` | `document_id`, `actor_id`, `action`, `reason`, `ip_hash`, `created_at` |
| `conversation_turns` | `conversation_id`, `role`, `text`, `input_type`, `urgency`, `speech_status`, `created_at` |
| `policy_catalog` | `title`, `region`, `eligibility`, `application_method`, `source_url`, `verified_at` |
| `conversation_summaries` | `senior_id`, `summary`, `risk_flags`, `sharing_scope`, `created_at` |
| `audit_logs` | `actor_id`, `action`, `resource_type`, `resource_id`, `reason`, `created_at` |

음성 원본과 TTS 결과는 기본적으로 장기 저장하지 않는다. 데모에서는 처리 후 삭제하거나 명시적 동의를 받은 경우에만 짧은 보관기간을 둔다.

### 13.1 가족관계증명서·권한 위임 동의서 저장 결정

결론은 **파일과 구조화 데이터의 혼합 저장**이다. 문서 이미지만 DB에 넣거나 모든 내용을 JSON만으로 바꾸는 두 극단을 사용하지 않는다.

| 문서 종류 | 정본/증적 | Supabase Storage | Postgres | MVP 처리 |
|---|---|---|---|---|
| 가족관계증명서 등 외부 발급 증빙 | 업로드한 원본 PDF/JPEG/PNG. 업로드 자체가 법적 검증 완료를 의미하지 않음 | `authority-documents` 비공개 버킷에 원본 보관 | 유형, 소유자, 발급/만료일, SHA-256, 크기, 검토 상태, 보관기한만 저장 | 합성·마스킹 문서만 사용. OCR 및 AI 전송 금지 |
| 외부 위임장·동의서 스캔 | 서명된 원본 파일 | 같은 비공개 버킷에 원본과 필요 시 안전한 미리보기 파생본 보관 | 위임 범위, 당사자, 검토자, 유효기간, 철회 상태, 원본 해시 저장 | `업로드됨`과 `기관 검토 완료`를 구분 |
| 앱 안에서 생성한 정보공유 동의 | 버전이 고정된 구조화 동의 레코드가 업무 정본 | 사람이 내려받을 수 있는 렌더링 PDF 스냅샷만 보관 | 동의 항목·목적·수신자·시작/만료·서명 시각·문구 버전·해시·철회 이력을 append-only로 저장 | 구조화 레코드로 즉시 권한 판정; 철회 즉시 RLS 반영 |

저장 구현 규칙은 다음과 같다.

- 파일 바이트나 Base64를 Postgres `bytea`/text 칼럼에 넣지 않는다. DB에는 검색·권한·무결성 확인용 메타데이터만 둔다.
- 버킷은 공개로 전환하지 않는다. 민감 원본은 매 요청 로그인·관계·동의·목적을 재확인하는 서버 인증 다운로드를 기본으로 한다. signed URL이 불가피한 미리보기는 60초 이하로 제한하고, 발급 후에는 만료 전 즉시 취소할 수 없다는 잔여 위험을 명시한다.
- object path는 `{senior_uuid}/{document_uuid}/original.{ext}`처럼 서버가 생성하며 이름·주민번호를 경로와 파일명에 넣지 않는다.
- 허용 형식은 MVP에서 PDF/JPEG/PNG, 앱 자체 상한은 파일당 5MB로 둔다. 확장자뿐 아니라 magic bytes, MIME, 실제 크기를 검증하고 실행성/암호화 PDF는 거부한다.
- 이미지 업로드는 가독성을 유지하는 범위에서 방향 보정·리사이즈·압축하고, 원본 SHA-256은 업로드 시 계산해 변조 감지에 사용한다.
- 원본을 브라우저에 inline 공개하지 않고 안전한 미리보기 또는 강제 다운로드를 사용한다. 미리보기 파생본도 원본과 같은 권한·보관정책을 적용한다.
- 임시 업로드는 24시간 뒤 삭제한다. 3일 MVP의 합성 문서는 데모 종료 후 24시간 안에 삭제한다. 실서비스의 활성·만료·반려 문서 보유기간은 처리 목적과 법적 근거별 정책을 확정하기 전에는 실제 문서를 받지 않는다.
- 삭제 작업은 Storage 원본·파생본과 DB 상태를 함께 처리하고 `document_access_logs` 및 삭제 감사 이벤트를 남긴다. legal hold가 있으면 삭제를 중단하고 사유를 기록한다.

### 13.2 Supabase Free 플랜 판단

2026-08-24 공식 가격표 기준 Free 플랜은 프로젝트당 DB 500MB, Storage 1GB, 단일 파일 최대 50MB를 제공하며 1주 비활성 시 프로젝트가 일시 정지될 수 있다. 본 앱은 5MB 앱 상한, 합성 문서, 짧은 보관, 원본 미중복으로 3일 데모를 수용한다. Storage 사용량 70%에서 경고하고 85%에서 새 업로드를 막는다. Free 플랜에는 자동 백업과 PITR이 없으므로 **실제 가족관계증명서·주민등록번호·건강 문서를 보관하는 운영 환경으로 간주하지 않는다**. 파일럿 전에는 유료 플랜, 백업·복구, 키 관리, 악성 파일 검사, 개인정보 영향·법률 검토를 완료해야 한다.

## 14. API 기능과 2026-08-24 실행 가능성 점검

판정은 실제 인증 호출 결과가 아니라 **공식 문서의 현재 제공 여부 + 저장소의 환경변수 이름 존재 여부**를 합친 것이다. 사용자가 API 호출을 금지했으므로 `조건부·미검증`을 `실행 확인`으로 과장하지 않는다.

| 서비스/API | 실제 기능 | 공식 문서 상태 | 현재 저장소 실행 준비 | 필요한 조치와 폴백 |
|---|---|---|---|---|
| OpenAI Responses API + `gpt-5.6-terra` | 텍스트 입력, 텍스트 답변, 의도 JSON, 함수 호출 | [현재 모델 카탈로그](https://developers.openai.com/api/docs/models/gpt-5.6-terra)에 제공 중. Structured Outputs와 function calling 지원 | **조건부·미검증.** `OPENAI_API_KEY`와 `OPENAI_PROJECT_ID` 이름은 있으나 값·모델 권한·결제·한도 미확인 | 프로젝트 대시보드에서 Terra 허용과 결제를 사람이 확인. 실패 시 시드 응답을 쓰되 `데모 데이터` 표시 |
| OpenAI Transcription + `gpt-transcribe` | 완료된 녹음 파일 또는 Realtime 턴을 한국어 텍스트로 전사 | [고정확도 전사 모델](https://developers.openai.com/api/docs/models/gpt-transcribe)로 제공 중 | **조건부·미검증.** §11.2의 동일한 `OPENAI_API_KEY`·`OPENAI_PROJECT_ID`를 사용하며 별도 키가 필요 없다. 다만 Free tier 미지원 표시로 결제·모델 권한 미확인 | 60초/5MB 제한, 타임아웃, 전사문 사용자 확인. 실패 시 텍스트 직접 입력 |
| OpenAI Speech + `gpt-4o-mini-tts` | 답변 텍스트를 음성으로 합성 | [Speech 모델](https://developers.openai.com/api/docs/models/gpt-4o-mini-tts)로 제공 중 | **조건부·미검증.** §11.2의 동일한 `OPENAI_API_KEY`·`OPENAI_PROJECT_ID`를 사용하며 별도 키가 필요 없다. 다만 프로젝트 권한·결제 미확인 | 서버 TTS 5초 초과/실패 시 브라우저 `speechSynthesis`; 긴급 문구는 로컬 고정 음원/문구 |
| OpenAI Realtime + `gpt-realtime-2.1-mini` | WebRTC/WebSocket 저지연 음성 입출력과 함수 호출 | [Realtime 모델](https://developers.openai.com/api/docs/models/gpt-realtime-2.1-mini)로 제공 중 | **조건부·MVP 제외.** 키 외 임시 세션·WebRTC 구현과 기기 테스트 필요 | Structured Outputs 미지원이므로 고위험 실행 판단에 사용하지 않고 P2 실험으로 격리 |
| Supabase Auth/Postgres/RLS/Realtime/Storage | 로그인, 관계·동의 데이터, 실시간 상태, 비공개 문서 파일 | [Next.js Quickstart](https://supabase.com/docs/guides/getting-started/quickstarts/nextjs)와 [Storage private bucket](https://supabase.com/docs/guides/storage/buckets/fundamentals) 제공 중 | **현재 구성 불가.** URL·publishable·secret 키가 모두 없음 | Supabase 프로젝트와 3개 환경변수 추가. 연결 전에는 로컬 repository adapter와 시드 데이터 사용 |
| 국립중앙의료원 전국 병·의원 찾기 OpenAPI | 시도·시군구·기관·진료과목 등으로 병·의원 검색, XML 응답 | [공공데이터포털](https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15000736)에 현재 게시, 무료, 개발/운영 자동승인, 개발 1,000건 | **현재 구성 불가.** `DATA_GO_KR_SERVICE_KEY` 없음 | 활용신청 후 서버 프록시에서 XML 정규화. 데모 직전 smoke test, 실패 시 기준일이 표시된 fixture |
| 충청남도 노인복지시설 OpenAPI | 시설명·상세주소·전화번호 검색, JSON/XML | [공공데이터포털](https://www.data.go.kr/data/15099688/openapi.do?recommendDataYn=Y)에 게시, 무료, 자동승인, 개발 10,000건 | **현재 구성 불가.** 공공데이터 키 없음 | 메타데이터 수정일이 2022-08-17이므로 응답 성공과 데이터 최신성을 별도 표시. 실패 시 fixture |
| Kakao Local API | 키워드/카테고리/좌표/반경 기반 병원·약국·장소 검색 | [Local API 문서](https://developers.kakao.com/docs/ko/local/dev-guide)에 REST API 키 인증 방식으로 제공 중 | **현재 구성 불가.** `KAKAO_REST_API_KEY` 없음 | MVP 선택 기능. REST 키를 서버 헤더에만 넣고 쿼터·저장/재사용 정책 확인. 공공 API로 폴백 |
| 정부24·보건복지부 안내 페이지 | 노인맞춤돌봄서비스 자격·방법·기관 안내 | [정부24 신청 안내](https://www.gov.kr/mw/AA020InfoCappView.do?CappBizCD=13520000045)와 [보건복지부 정책](https://www.mohw.go.kr/menu.es?mid=a10712010400) 페이지 접근 가능 | **API 아님.** 제3자 자동 제출 연동으로 사용할 수 없음 | 출처 링크·전화·사회복지사 업무 초안만 제공. 접수 완료로 표시 금지 |
| 브라우저 Geolocation | 사용자가 허용한 현재 좌표 조회 | [Geolocation API](https://developer.mozilla.org/en-US/docs/Web/API/Geolocation_API)는 널리 지원되나 HTTPS와 명시적 권한 필요 | **조건부 실행 가능.** 별도 키 불필요 | 권한 거부·타임아웃·낮은 정확도에는 주소 직접 입력. 위치를 상시 추적하지 않음 |
| 브라우저 `speechSynthesis` | 기기 기본 음성으로 텍스트 읽기 | [SpeechSynthesis](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesis)는 널리 지원 | **폴백으로 실행 가능.** 별도 키 불필요, 한국어 음성 품질은 기기 의존 | 기능 감지 후 사용하고 실기기에서 음성 존재·재생 정책 테스트 |
| `tel:`/문자 딥링크 | 전화·문자 앱 화면 열기 | `tel:` URI는 [RFC 3966](https://www.rfc-editor.org/info/rfc3966)으로 정의, 실제 처리는 기기·브라우저 의존 | **조건부 실행 가능.** 데스크톱/통화 앱 없는 기기에서는 실패 가능 | 데모는 발신 직전 화면까지만. 실제 신고·문자 접수 성공으로 표시하지 않음 |

### 14.1 MVP 확정 선택

- **기본 AI 경로:** `gpt-transcribe → gpt-5.6-terra Responses → gpt-4o-mini-tts`, 브라우저 TTS 폴백. 세 호출 모두 `OPENAI_API_KEY` 하나와 `OPENAI_PROJECT_ID` 하나를 공유한다.
- **데이터·권한:** Supabase Auth/Postgres/RLS/Realtime/Storage. 연결 전에도 테스트 가능한 repository interface와 in-memory fake를 먼저 만든다.
- **실데이터 검색:** 국립중앙의료원 API와 충남 시설 API. 두 연동 모두 fixture 기반 계약 테스트와 장애 폴백을 필수로 한다.
- **후순위:** Realtime 음성과 Kakao Local은 핵심 수직 시나리오가 모두 통과한 뒤에만 추가한다.
- **제외:** 음식 사진 분석과 모든 이미지 입력 AI 경로, 정부24 자동 제출, 119 자동 신고, 법적 문서 자동 검증, 실제 민감 문서 OCR.
- **MCP 판단:** 이번 MVP는 몇 개의 고정 서버 함수가 더 단순하고 안전하다. 기관과 도구가 많아지는 2단계부터 승인된 MCP 서버를 검토한다.

### 14.2 출시 직전 실행 확인 체크

문서상 제공과 실제 계정 실행은 다르므로, 키 소유자가 로컬에서 아래의 비파괴 smoke test를 수행해야 `실행 확인`으로 승격한다.

1. 각 환경변수는 값 자체를 로그에 남기지 않고 존재 여부만 검사한다.
2. OpenAI는 **동일한 `OPENAI_API_KEY`와 `OPENAI_PROJECT_ID` 헤더로** 텍스트(Responses), 전사(Transcription), 음성(Speech) **3종 각각에 최소 요청 1건**을 보내 2xx, 모델 접근 오류(프로젝트 허용 목록·키 스코프), 잔액/한도 오류를 구분한다. 세 호출이 모두 2xx여야 §11.2의 단일 자격증명 구성이 `실행 확인`으로 승격된다. 하나라도 모델 접근 오류가 나면 키를 늘리지 말고 프로젝트 설정을 먼저 교정한다.
3. Supabase는 테스트 사용자로 로그인하고 본인 행 조회, 타인 행 차단, 비공개 버킷 업로드/다운로드/삭제를 확인한다.
4. 두 공공 API는 최소 검색 1건의 상태코드·스키마·한글 인코딩을 확인하고 fixture와 contract가 맞는지 비교한다.
5. Kakao Local을 활성화한 경우에만 테스트하고, 미설정이면 기능 플래그를 꺼 숨긴다.
6. 이 smoke test는 이번 PRD 수정 작업에서는 실행하지 않는다.

## 15. 119 및 서비스 신청의 현실적 설계

### 15.1 119

본 PRD에서 확인한 공개 문서 기준으로, 일반 제3자 웹앱이 119 신고 접수를 완료하는 공개 API를 전제로 하지 않는다. 소방청은 전화 외에 문자·영상통화·스마트폰 앱 등 다매체 신고를 안내한다. 따라서 MVP는 다음을 구현한다.

- AI가 긴급 표현을 감지해 즉시 긴급 화면 표시
- 위치와 증상 발화를 신고용 한 문장으로 정리
- 사용자가 `119 전화하기` 또는 지원되는 기기에서 `119 문자 작성` 실행
- 동시에 동의된 가족·사회복지사에게 앱 내부 알림 생성
- 화면에는 `전화 화면 열림`, `가족 알림 전달됨`처럼 실제 확인 가능한 상태만 표시

실서비스에서 자동 신고를 추진하려면 소방기관 협의, 공식 연계 규격, 오탐·미탐 검증, 장애 대응, 위치 정확도, 본인 동의와 법률 검토가 선행되어야 한다.

### 15.2 병원동행·복지서비스

- AI가 자격요건과 누락 정보를 질문해 **신청 초안**을 만든다.
- 노인이 내용을 확인하면 사회복지사 업무함에 전달한다.
- 사회복지사가 실제 수행기관에 전화·공식 시스템으로 접수하고 결과를 기록한다.
- 연계 API가 확보된 기관만 이후 `최종 제출` 기능을 활성화한다.

## 16. 개인정보·안전·윤리 요구사항

건강정보는 개인정보 보호법상 민감정보에 해당하므로 별도 동의와 보호가 필요하다.

- 목적별·항목별 동의와 언제든 철회 가능한 UI
- 최소 수집: 데모에 필요하지 않은 주민등록번호, 가족관계증명서 실물, 상세 진료기록은 받지 않음
- 역할·관계·동의 기반 RLS 정책
- 가족/사회복지사 계정은 2단계 인증을 확장 요구사항으로 설정
- 고위험 정보 열람에는 사유와 감사 로그를 남기는 `긴급 열람(break-glass)` 정책
- 저장 파일은 비공개 버킷과 60초 이하의 짧은 만료 URL 사용. URL과 토큰을 로그·분석 도구에 남기지 않음
- 원본 음성과 음성 합성 결과의 기본 미보관, 보관기간 명시
- AI 입력 전 불필요한 식별정보 제거
- 권한 증빙 문서는 MVP에서 OpenAI 또는 제3자 OCR로 전송하지 않음
- OpenAI Responses 요청은 `store: false`를 기본으로 하되, 이것이 악용 모니터링 로그까지 즉시 삭제한다는 뜻은 아님을 개인정보 처리방침에 반영
- Supabase secret/service-role 키는 RLS를 우회하므로 서버의 최소 관리 경로에서만 사용하고 일반 사용자 요청은 사용자 JWT + RLS로 처리
- 가족관계증명서에 포함될 수 있는 주민등록번호는 동의만으로 무조건 처리할 수 있는 정보로 가정하지 않으며, 구체적 법적 근거와 암호화·접근통제가 확정되기 전 실문서 업로드를 비활성화
- 개발/데모 데이터와 실제 개인정보 완전 분리
- AI 결과에 `AI 생성`, 확신도, 출처, 사람이 확인해야 할 항목 표시
- 위기 분류 실패를 대비해 긴급 버튼은 AI와 무관하게 항상 접근 가능

## 17. 노인 친화 UX 기준

- 본문 글자 최소 20px, 주요 버튼 글자 24px 이상 권장
- 터치 영역 최소 48×48px, 핵심 버튼은 더 크게 설계
- 고대비 색상과 색 외의 아이콘·텍스트 병행
- 한 화면 한 과업, 선택지는 최대 2개
- 전문용어 대신 “신청서를 만들었어요. 담당자에게 보낼까요?” 같은 쉬운 문장
- 모든 AI 응답은 텍스트와 음성을 함께 제공하고 `다시 듣기`, `일시정지`, `그만 듣기` 버튼을 둠
- 사용자가 말하기를 시작하거나 새 답변을 받으면 이전 음성을 중단해 겹침을 방지
- 음성 출력 실패가 대화 실패로 오인되지 않도록 텍스트를 먼저 확정 표시하고 재생 상태를 별도로 알림
- 제한시간 자동 종료를 피하고, 불가피하면 충분히 연장 가능하게 함
- 오류 시 코드 대신 “인터넷 연결이 불안해요. 119 전화 버튼은 계속 사용할 수 있어요.” 표시
- 음성만 강제하지 않고 터치와 텍스트 대체 수단 제공

## 18. 가상 이용 시나리오

### 18.1 노인 시나리오 1 — 위기 발화

1. 어르신이 “가슴이 조이고 숨쉬기가 힘들어”라고 말한다.
2. AI가 긴급 발화로 분류하고 일반 대화를 중단한다.
3. 화면에 `119 전화하기`가 크게 나타나고 “가슴 통증과 호흡 곤란, 현재 위치 ○○”라는 신고 문장을 보여준다.
4. 어르신이 전화 버튼을 누르는 동시에 가족과 담당 사회복지사에게 위기 알림이 생성된다.
5. 가족이 `확인함`을 누르면 사회복지사 화면에도 즉시 반영된다.

### 18.2 노인 시나리오 2 — 병원동행 요청

1. 어르신이 “다음 주 화요일 충남대병원 갈 때 같이 갈 사람이 필요해”라고 말한다.
2. AI가 날짜, 목적지, 동행 필요를 추출하고 시간을 되묻는다.
3. 어르신이 내용을 듣고 “맞아, 보내줘”라고 확인한다.
4. 사회복지사 업무함에 병원동행 요청 초안이 생성되고 어르신 화면에는 “담당자가 확인 중이에요”가 표시된다.

### 18.3 부양가족 시나리오 1 — 위기 알림 대응

1. 딸 이지현 씨가 아버지의 위기 알림을 받는다.
2. 가족 화면에서 발화 원문, 감지 시각, 위치, 119 전화 화면을 열었는지 확인한다.
3. `아버지에게 전화`를 누르고, 이어 `사회복지사와 대응 중` 상태를 선택한다.
4. 중복 연락을 줄이도록 모든 처리자가 같은 타임라인을 본다.

### 18.4 부양가족 시나리오 2 — 주간 상태 확인

1. 이지현 씨가 주간 요약에서 최근 병원동행 요청 1건과 복지 정보 조회 2건을 확인한다.
2. 말동무 대화 원문은 보이지 않고, 아버지가 공유에 동의한 “오늘 기분 괜찮음” 요약만 본다.
3. 미처리 요청의 담당자와 예상 연락일을 확인하고 불필요한 전화 확인을 줄인다.

### 18.5 부양가족 시나리오 3 — 대리 문서와 결정 요청

1. 가족이 등록된 위임 문서의 검토 상태와 만료 예정일을 확인한다.
2. 앱은 “업로드됨”과 “기관 검토 완료”를 구분해 보여준다.
3. 위기 상황에서 사회복지사가 특정 결정 요청을 보내면 가족은 상황·선택지·기한을 읽고 응답한다.
4. 응답자, 시각, 근거가 기록되며 AI가 결정을 대신하지 않는다.

### 18.6 사회복지사 시나리오 1 — 아침 위험도 분류

1. 박사회복지사가 10시에 로그인한다.
2. 담당 노인 목록 맨 위에 미확인 위기 1건, 주의 2건, 신규 요청 3건이 보인다.
3. 위기 건을 먼저 열어 가족 확인 상태와 최근 정보를 확인하고 대응 메모를 남긴다.
4. 상태를 `후속 확인 필요`로 바꾸면 가족 화면에도 쉬운 문구로 반영된다.

### 18.7 사회복지사 시나리오 2 — 서비스 신청 보조

1. 업무함에서 병원동행 요청을 연다.
2. AI가 상담 내용으로 신청 초안, 누락 필드, 가능한 지역 서비스를 제안한다.
3. 사회복지사가 대상 조건과 일정을 확인하고 수정한 뒤 `기관 연결` 상태로 변경한다.
4. 어르신에게는 “담당자가 동행 기관을 찾고 있어요”라고 표시된다.

### 18.8 사회복지사 시나리오 3 — 위기 후 기록 정리

1. 위기 대응이 끝난 뒤 사회복지사가 발화, 연락, 가족 응답, 처리 결과가 담긴 타임라인을 연다.
2. AI가 사례기록 초안을 만들지만 사실과 추정을 구분해 표시한다.
3. 사회복지사가 내용을 검토·수정해 저장하고 후속 방문 일정을 만든다.
4. 시스템은 누가 AI 초안을 승인했는지 감사 로그에 남긴다.

### 18.9 노인 시나리오 3 — 텍스트로 묻고 음성으로 듣기

1. 주변 소음 때문에 음성 입력이 어려운 어르신이 “내일 복지관 몇 시에 열어요?”를 키보드 또는 빠른 문구로 입력한다.
2. 챗봇은 출처와 확인일이 포함된 짧은 답변을 화면에 먼저 표시한다.
3. 같은 내용이 한국어 음성으로 자동 재생되고, 어르신은 `다시 듣기`를 눌러 반복한다.
4. 서버 TTS가 실패하면 기기 음성으로 읽고, 두 방식이 모두 실패해도 화면 텍스트와 전화 문의 버튼은 유지된다.

## 19. 3일 개발 일정

점심·휴식 시간을 제외하면 실제 개발 가능 시간은 약 21시간으로 가정한다.

### Day 1 — 기반과 세 화면 (10:00–18:00)

| 시간 | 작업 | 완료 결과 |
|---|---|---|
| 10:00–11:00 | 문제·데모 흐름 확정, 테스트 러너·품질 게이트 설정 | 2개 핵심 수직 시나리오와 첫 실패 테스트 확정 |
| 11:00–13:00 | Next.js/Supabase 설정, 스키마·RLS 테스트 우선 작성 | 로그인과 기본 테이블/RLS |
| 13:00–14:00 | 점심 |  |
| 14:00–16:00 | 역할별 라우트와 공통 디자인 | 노인·가족·사회복지사 화면 골격 |
| 16:00–18:00 | 요청/위기 CRUD와 시드 데이터 | 세 화면에서 같은 사건 조회 |

### Day 2 — AI와 실제 데이터 (10:00–18:00)

| 시간 | 작업 | 완료 결과 |
|---|---|---|
| 10:00–12:00 | 전사·의도 구조화 계약 테스트 후 구현 | 말로 서비스 요청 생성 |
| 12:00–13:00 | 의도 분류 정확도 보강과 누락 필드 질문 흐름 | 20개 발화 회귀 테스트 통과 |
| 13:00–14:00 | 점심 |  |
| 14:00–16:00 | 병원·충남 시설 API 연결 | 실제 검색 결과 카드 |
| 16:00–17:00 | TTS·브라우저 폴백 테스트 후 구현 | 텍스트·음성 입력 모두 음성 답변 |
| 17:00–18:00 | 긴급 흐름과 Realtime 알림 | 노인 발화가 가족/복지사에 전달 |

### Day 3 — 안전, 완성도, 발표 (10:00–18:00)

| 시간 | 작업 | 완료 결과 |
|---|---|---|
| 10:00–12:00 | RLS·동의·감사 로그 검증 | 역할 간 데이터 누출 차단 |
| 12:00–13:00 | 노인 접근성·오류 복구 개선 | 큰 글씨, 다시 듣기, 네트워크 오류 UI |
| 13:00–14:00 | 점심 |  |
| 14:00–15:30 | 통합 테스트와 버그 수정 | 2개 핵심 시나리오 성공 |
| 15:30–16:30 | 데모 데이터·발표 대본 준비 | 5분 데모 완성 |
| 16:30–18:00 | 리허설, 백업 영상·스크린샷 | 인터넷 장애 대비 자료 확보 |

### 일정 보호 규칙

- Day 1 11시 이후 새로운 핵심 기능을 추가하지 않는다.
- Realtime 음성은 MVP 기본 범위에 넣지 않고 녹음 후 전사 방식을 먼저 완성한다.
- 실제 기관 접수 대신 명확히 표시된 `데모 접수함`을 사용한다.
- 발표 2시간 전부터 기능 추가를 중단하고 안정화만 한다.

## 20. 테스트 계획

### TDD 실행 규칙

- 각 사용자 스토리는 `실패하는 테스트(Red) → 최소 구현(Green) → 중복 제거·명명 개선(Refactor)` 순서로 진행한다.
- Green 전에 기능 코드를 먼저 작성하지 않는다. 버그는 재현 테스트를 먼저 추가한 뒤 수정한다.
- 외부 API의 기본 테스트는 mock/fixture 기반 단위·계약 테스트이며 실제 키를 요구하지 않는다. 실제 호출은 별도 `smoke` 명령과 명시적 환경 플래그로만 실행한다.
- 각 단계 종료 조건은 관련 테스트, 타입 검사, lint, build 통과다. 실패한 상태를 커밋하거나 다음 단계로 넘기지 않는다.
- 테스트는 정상 경로뿐 아니라 권한 거부, 타임아웃, 잘못된 스키마, 빈 결과, TTS 실패, 중복 제출을 포함한다.

### 기능 테스트

- 역할별 로그인과 화면 접근
- 요청 생성 → 사회복지사 처리 → 노인/가족 상태 반영
- 긴급 이벤트 생성 → 두 역할에 실시간 알림
- 공공 API 실패 시 대체 메시지와 재시도
- 텍스트·음성 입력 각각에서 답변 텍스트와 TTS 요청 생성
- 서버 TTS 실패 또는 5초 초과 시 브라우저 TTS 폴백, 다시 듣기, 재생 중단
- 비공개 문서 업로드 → 권한 있는 열람 → 동의 철회/삭제 후 접근 차단

### 안전 테스트

- 긴급 발화 20개와 비긴급 유사 발화 20개 비교
- “가슴이 아프지 않아” 같은 부정 표현 테스트
- AI가 승인 없이 높은 위험도 도구를 호출하려 할 때 서버 차단
- 동의 철회 후 가족 계정의 즉시 접근 차단
- 다른 노인의 URL·ID를 직접 입력하는 권한 우회 테스트
- 조작된 `assistant_turn_id`로 다른 노인의 답변 음성 생성을 요청할 때 403 반환
- 확장자와 MIME/magic bytes가 다른 파일, 5MB 초과 파일, 공개 버킷 접근 시도 차단
- 가족관계증명서·위임장 원본이 OpenAI mock 호출 payload에 포함되지 않는지 검사

### 접근성 테스트

- 스마트폰 한 손 조작
- 200% 확대에서 잘림 여부
- 색을 구분하지 못해도 상태를 이해할 수 있는지 확인
- 음성 권한·위치 권한 거부 후 대체 경로 확인
- 한국어 시스템 음성이 없는 기기, 자동 재생 차단, 이어폰/무음 상태에서 텍스트 대체와 상태 안내 확인
- 스크린 리더가 답변 텍스트를 중복 낭독하지 않도록 live region과 TTS 동시 동작 확인
- 60대 이상 테스트 사용자 1명에게 핵심 과업 관찰 테스트

## 21. 성공 지표

### MVP 기술 지표

- 핵심 데모 시나리오 2개 연속 성공률 100%
- 실시간 상태 반영 목표 3초 이내
- 사전 정의 발화 의도 정확도 85% 이상
- 긴급 테스트 문구 재현율 100% 목표
- 승인 없는 고위험 도구 실행 0건
- 역할 간 권한 테스트 데이터 누출 0건
- 두 입력 방식(음성·텍스트)의 AI 답변에서 텍스트 표시 성공률 100%, 음성 또는 명시적 폴백 제공률 100%
- 권한 없는 문서·TTS 답변 ID 직접 접근 성공 0건

### 사용자 가치 지표

- 노인이 도움 요청을 시작하기까지 1회 터치 이하
- 병원동행 요청 완료까지 질문 3개 이하
- 가족이 위기 핵심 정보를 이해하는 데 30초 이하
- 사회복지사가 요청 초안을 검토하는 시간이 수기 작성 대비 감소

## 22. 주요 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| AI가 긴급상황을 놓침 | 매우 높음 | 고정 긴급 버튼, 규칙 기반 키워드와 모델 분류 병행, 높은 재현율 우선 |
| AI가 비긴급을 긴급으로 오탐 | 중간 | 사용자 확인, 발화 원문 표시, 가족/복지사 상태 공유 |
| 개인정보 과다 공유 | 매우 높음 | RLS, 동의 범위, 최소 수집, 감사 로그, 원문 대화 비공유 |
| 민감정보가 OpenAI 요청·로그에 포함 | 매우 높음 | 서버 마스킹, `store: false`, payload 테스트, 권한 문서 AI 전송 금지, 운영 전 데이터 처리 검토 |
| 권한 문서 유출·위조·용량 초과 | 매우 높음 | 비공개 Storage, RLS, 짧은 URL, SHA-256, 5MB 제한, 70/85% 용량 경보, 실제 문서 MVP 금지 |
| TTS 지연·실패·음성 겹침 | 높음 | 텍스트 우선 표시, 5초 폴백, Web Speech API, 취소 가능한 단일 재생 큐, 고정 긴급 음원 |
| 공공 API 지연/장애 | 중간 | 서버 타임아웃, 최근 검증된 데모 데이터, 출처·기준일 표시 |
| 3일 범위 초과 | 높음 | 두 개 수직 시나리오 외 기능 동결, 폴백 방식 사전 결정 |
| 실제 접수로 오인 | 높음 | 모든 데모 화면에 `챌린지 데모—실제 접수 아님` 배지 |

## 23. 발표용 차별점

일반적인 노인 챗봇은 질문에 답하고 끝난다. 돌봄이음 AI의 차별점은 **한 사람의 말 한마디가 세 역할의 실제 업무 흐름으로 연결된다는 것**이다.

- 노인에게는 메뉴가 아닌 대화와 음성
- 가족에게는 감시가 아닌 동의 기반 안심 정보
- 사회복지사에게는 대체가 아닌 반복 행정업무 보조
- 위기에는 AI의 판단만 믿지 않는 전화·사람·감사 로그의 안전망

### 5분 데모 순서

1. 노인이 “병원 갈 때 같이 갈 사람이 필요해”라고 말함
2. AI가 필요한 정보를 묻고 요청 초안 생성
3. 사회복지사 화면에 요청이 실시간 도착하고 AI 신청 초안 확인
4. 긴급 발화를 입력해 119 연결 화면과 가족/사회복지사 동시 알림 시연
5. 마지막에 동의·권한·감사 로그를 보여 안전 설계 강조

## 24. 출시 이후 확장 로드맵

### 1단계 — 챌린지 이후 1개월

- 실제 사용자 인터뷰와 고령자 사용성 테스트
- 지역별 정책 데이터 검증 프로세스
- 가족·사회복지사 2단계 인증
- 알림톡/SMS 알림과 실패 재전송
- PWA 설치와 제한적 오프라인 화면

### 2단계 — 기관 파일럿 3개월

- 협력 복지관의 병원동행 업무 프로세스 연동
- 문서 OCR과 위임 문서 만료 관리
- 상담 녹취 동의, 자동 요약, 기관 서식 출력
- 케이스별 AI 품질 평가와 위기 분류 감사
- 수행기관 승인형 MCP 또는 표준 커넥터 검토

### 3단계 — 정식 서비스

- 지자체·의료기관·장기요양기관과 공식 API/업무 협약
- 웨어러블·IoT는 별도 동의와 의료·보안 검토 후 추가
- 다국어·방언·저시력·청각장애 접근성 확대
- 재난 상황 장애 대응, 24시간 모니터링, 개인정보 영향평가

## 25. 확정된 결정과 남은 확인

### 확정

1. 데모 지역은 충남 중심으로 제한한다.
2. 노인 건강 주의사항과 권한 문서는 합성 시드 데이터만 사용한다.
3. 음성은 `녹음 → gpt-transcribe → gpt-5.6-terra Responses → gpt-4o-mini-tts`를 기본으로 하고 브라우저 TTS를 폴백으로 둔다. Realtime은 P2다.
4. 노인의 입력이 텍스트여도 모든 AI 답변은 텍스트와 음성을 함께 제공한다.
5. 119는 전화/문자 화면 열기 직전까지만 시연하며 실제 발신·자동 신고를 하지 않는다.
6. 가족관계증명서 등 외부 문서는 비공개 Storage 원본 + Postgres 메타데이터, 앱 동의는 구조화 레코드 + PDF 증적으로 저장한다. Base64/BLOB DB 저장은 금지한다.
7. 정부24·복지서비스는 신청 초안과 담당자 연결만 제공하며 자동 접수로 표시하지 않는다.
8. 음식 사진 분석은 MVP에서 제외한다. 노인의 입력은 음성과 텍스트 두 가지이며 이미지 업로드 경로를 만들지 않는다.
9. AI 호출 자격증명은 `OPENAI_API_KEY`와 `OPENAI_PROJECT_ID` 두 개로 통합하고, 모델은 요청마다 `model` 필드로 지정한다. Supabase·공공데이터·Kakao 키는 별도로 유지한다.

### 개발 시작 전 사람이 확인할 항목

1. 서비스 대표 색상과 최종 명칭
2. OpenAI 프로젝트의 결제, `gpt-5.6-terra`, `gpt-transcribe`, `gpt-4o-mini-tts` 모델 허용 여부, 그리고 `OPENAI_API_KEY`가 세 엔드포인트를 모두 호출할 수 있는 권한인지(restricted 스코프로 발급되지 않았는지) 확인
3. Supabase 프로젝트 생성과 publishable/secret 키 발급
4. 공공데이터포털 활용신청과 서비스 키 발급, 데모 전 smoke test
5. GitHub 원격 저장소 URL과 `main` 브랜치 push 권한

## 26. 공식 참고자료

- [OpenAI Responses API — 텍스트·이미지 입력, 함수 및 MCP 도구 호출](https://developers.openai.com/api/reference/cli/resources/responses/methods/create)
- [OpenAI 모델 목록 — 현재 멀티모달·Realtime·전사·음성 모델](https://developers.openai.com/api/docs/models)
- [GPT-5.6 Terra — 텍스트·이미지·구조화 출력·함수 호출](https://developers.openai.com/api/docs/models/gpt-5.6-terra)
- [GPT-Transcribe — 고정확도 음성 전사](https://developers.openai.com/api/docs/models/gpt-transcribe)
- [GPT-4o mini TTS — 텍스트 음성 합성](https://developers.openai.com/api/docs/models/gpt-4o-mini-tts)
- [GPT-Realtime-2.1 mini — 선택형 실시간 음성](https://developers.openai.com/api/docs/models/gpt-realtime-2.1-mini)
- [OpenAI API 데이터 제어와 보관](https://platform.openai.com/docs/models/default-usage-policies-by-endpoint)
- [Next.js App Router](https://nextjs.org/docs/app)
- [Next.js Route Handlers](https://nextjs.org/docs/app/getting-started/route-handlers)
- [Supabase Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [Supabase Realtime Authorization](https://supabase.com/docs/guides/realtime/authorization)
- [Supabase Next.js 환경변수](https://supabase.com/docs/guides/getting-started/quickstarts/nextjs)
- [Supabase API 키 유형 — publishable/secret](https://supabase.com/docs/guides/getting-started/api-keys)
- [Supabase Private Storage와 signed URL](https://supabase.com/docs/guides/storage/buckets/fundamentals)
- [Supabase Storage 접근 제어](https://supabase.com/docs/guides/storage/security/access-control)
- [Supabase Free 플랜 한도](https://supabase.com/pricing)
- [국립중앙의료원 전국 병·의원 찾기 OpenAPI](https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15000736)
- [충청남도 노인복지시설 조회 OpenAPI](https://www.data.go.kr/data/15099688/openapi.do?recommendDataYn=Y)
- [Kakao Local API](https://developers.kakao.com/docs/ko/local/dev-guide)
- [정부24 노인맞춤돌봄서비스 신청 안내](https://www.gov.kr/mw/AA020InfoCappView.do?CappBizCD=13520000045)
- [보건복지부 노인맞춤돌봄서비스](https://www.mohw.go.kr/menu.es?mid=a10712010400)
- [소방청 119 다매체 신고 안내](https://www.nfa.go.kr/nfa/news/pressrelease/press/?cntId=1902&mode=view&pageIdx=1&searchCondition=all)
- [브라우저 Geolocation API](https://developer.mozilla.org/en-US/docs/Web/API/Geolocation_API)
- [브라우저 SpeechSynthesis API](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesis)
- [개인정보 보호법 제23조 민감정보 처리 제한](https://www.law.go.kr/lsLinkCommonInfo.do?lsJoLnkSeq=1029335401)
- [개인정보 보호법 제24조의2 주민등록번호 처리 제한](https://www.law.go.kr/lsLinkCommonInfo.do?lsJoLnkSeq=1006184231)
- [개인정보 보호법 제29조 안전조치의무](https://law.go.kr/LSW/lsInfoP.do?lsiSeq=270351)
